{
  "name": "envitro",
  "description": "Load environment from package.json.",
  "repository": "git://github.com/joshrtay/envitro.git",
  "version": "1.0.0",
  "license": "MIT",
  "main": "lib/index.js",
  "scripts": {
    "postversion": "git push && git push --tags && npm publish --access=public"
  },
  "dependencies": {
    "@f/foreach": "^1.2.2",
    "@f/is-undefined": "^1.1.1"
  },
  "devDependencies": {
    "tape": "^4.2.2"
  }
}
